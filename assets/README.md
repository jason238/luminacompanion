# Website assets

Drop files into the folders below. Pages read these paths once wired in later tasks.

## Pilot photos — `images/pilot/`

**Where they appear on the site (from `lumina_website_v3.md`):**

| Use | Page / section | Notes |
|---|---|---|
| **Primary** | Home → “Real families” (social proof) | All **three** photos + first-name captions |
| Optional | Home hero | Pick the warmest / least “product demo” of the three |
| Optional | Pilot Program hero | One family photo **or** App “All is well” screenshot |
| **Share / OG** | Site-wide Open Graph | Copy the chosen hero photo into `images/og/` (see below) |

**Present (ready for Task 1.1):**

```
assets/images/pilot/
  grandma-tuen.png      # “Grandma Tuen, 75 — reading a memory…”
  grandma-xie.png       # “Grandma Xie, 87 — video calling…”
  michael.png           # “Michael, 67 — a quiet afternoon…”
```

Compress in Task 2.3 (files are currently large PNGs).

## Open Graph / link preview — `images/og/`

```
assets/images/og/
  og-default.png        # copy of the warmest Pilot photo (or a designed card later)
```

Used when someone shares `luminacompanion.ca` on iMessage / LinkedIn / etc. (Task 2.1).

## Product photos — `images/product/`

```
colmi-ring.png          # Lumina Wearable (plain background)
health-scale.png        # optional, when available
```

## Diagrams — `images/diagrams/`

```
vision-ecosystem.png    # About + Product “vision” diagram (core vs roadmap)
```

## Screenshots — `screenshots/`

```
hub-home-redacted.png   # Lumina Hub home (redacted)
app-home-redacted.png   # Family App “All is well” (name placeholder OK)
```

## Icons — `icons/`

How-it-works step icons (can be simple SVG later; not required for first ship).

---

**Rules:** real Pilot photos and redacted Hub/App shots only — no scaffold/dev UI screenshots on the marketing site.
